import java.util.scanner;
public class count{
public Static void main(String[]args){
    Scanner Scr=new Scanner(System.in);
    int size=kbd.next
     int keyele=kbd.nextInt();
     int count=0;
     for(int i=0;i<size;i++){
        arr[i]=scr.nextInt();
        {
            if(arr[i]<keyele)
            count++;  
        }
        System.out.print(count);
     }
}